sequenceDiagram
    participant User as Người dùng
    participant Blazor as WebBlazor (UI)
    participant API as GroupController
    participant Service as UsGroupService
    participant DB as SQL Server

    User->>Blazor: Tìm kiếm/Chuyển trang Nhóm
    Blazor->>API: GET /api/Group/paginated 
    API->>Service: GetPaginated(request) 
    Service->>DB: Query UsGroups (Active)
    alt Có từ khóa tìm kiếm (SearchTerm)
        Service->>DB: Lọc theo Name hoặc Note
    end
    Service->>DB: Sắp xếp (Sort) & Tính tổng số bản ghi
    Service->>DB: Phân trang (Skip/Take)
    DB-->>Service: Danh sách Group Entities
    Service->>Service: Mapping sang UsGroupViewModel
    Service-->>API: Trả về PaginatedResponse<UsGroupViewModel>
    API-->>Blazor: 200 OK
    Blazor-->>User: Hiển thị danh sách Nhóm lên Table